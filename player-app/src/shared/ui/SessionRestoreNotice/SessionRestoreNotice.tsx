export interface SessionRestoreNoticeProps {
  className?: string
}
function SessionRestoreNotice({ className }: SessionRestoreNoticeProps) {
  return <div className={className} />
}
export default SessionRestoreNotice
